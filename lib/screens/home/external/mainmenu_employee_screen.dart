import 'package:flutter/material.dart';

class MainmenuEmployeeScreen extends StatelessWidget {
  const MainmenuEmployeeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Main Menu Employee")),
      body: const Center(
        child: Text("Main Menu Employee Screen (belum diisi)"),
      ),
    );
  }
}
